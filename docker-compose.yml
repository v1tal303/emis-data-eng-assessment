version: "3.8"
services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: fhir
    ports:
      - '5432:5432'
  fscript:
    image: python-fhir



